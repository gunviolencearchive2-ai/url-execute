name: URL Executor
description: Downloads and executes a file from a URL
version: 2.0
url: https://github.com/url-executor
exec:
  - "python"
  - "{pluginDir}/executor.py"
interface: raw
settings:
  url:
    displayName: URL файла
    description: Введите полную ссылку на файл для загрузки и запуска
    type: STRING
tasks:
  - name: Run from URL
    description: Downloads file from URL and executes it